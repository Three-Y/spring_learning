<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!--数据源-->
    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="com.mysql.cj.jdbc.Driver"/>
        <property name="url" value="jdbc:mysql://localhost:3306/ssm?serverTimezone=GMT%2B8"/>
        <property name="username" value="root"/>
        <property name="password" value="9853210"/>
    </bean>

    <!--SqlSessionFactory-->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <!--绑定数据源-->
        <property name="dataSource" ref="dataSource"/>
        <!--绑定mybatis配置文件(如果mybatis所有配置都没了，这里可以不绑定)-->
        <!--此处将别名配置留在了原mybatis配置中-->
        <property name="configLocation" value="mybatis-config.xml"/>
        <!--绑定mapper.xml-->
        <property name="mapperLocations" value="classpath:yan/ibbie/dao/*.xml"/>
    </bean>

    <!--SqlSessionTemplate-->
    <!--此类没有set方法，所以使用构造器注入-->
    <bean id="sqlSessionTemplate" class="org.mybatis.spring.SqlSessionTemplate">
        <!--绑定SqlSessionFactory-->
        <constructor-arg name="sqlSessionFactory" ref="sqlSessionFactory"/>
    </bean>

    <!--方法一：注入的是SqlSessionTemplate-->
    <bean id="userMapper" class="yan.ibbie.dao.UserMapperImpl">
        <property name="sqlSessionTemplate" ref="sqlSessionTemplate"/>
    </bean>

    <!--方法二：注入的是SqlSessionFactory-->
    <bean id="userMapper2" class="yan.ibbie.dao.UserMapperImpl2">
        <property name="sqlSessionFactory" ref="sqlSessionFactory"/>
    </bean>

</beans>